class Package(object):
    def __init__(self, lockfile):
        """
        A lockfile can be:

        - filesystem path
        - parsed lockfile JSON
        - lockfile URI
        """
        pass
